#1. Can you organize the items by price?
SELECT *
FROM superstore
ORDER BY price;

#2. How much is the sum of all prices in the superstore?
SELECT SUM(price)
FROM superstore;

#3. What is the average price in the Kitchen Supplies category?
SELECT AVG(price)
FROM superstore
WHERE category="Kitchen Supplies";

#4. How many Wireless Earbuds are in stock?
SELECT SUM(stock_quantity)
FROM superstore
WHERE item_name="Wireless Earbuds";
